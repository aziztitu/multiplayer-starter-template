// <auto-generated>
// Serialization for TestNestedData
// </auto-generated>
using Unity.Netcode;
using System;
using Unity.Collections;
using System.Linq;

    public partial struct TestNestedData : INetworkSerializable, IEquatable<TestNestedData>
    {
        public void NetworkSerialize<T>(BufferSerializer<T> serializer) where T : IReaderWriter
        {
            serializer.SerializeValue(ref Health);
            serializer.SerializeValue(ref Weight);
            serializer.SerializeValue(ref IsAlive);
            serializer.SerializeValue(ref Position);
            serializer.SerializeValue(ref Rotation);
            serializer.SerializeValue(ref TransportMessageType);
            serializer.SerializeValue(ref Name);

            // FixedItems1
            if (serializer.IsWriter)
            {
                var writer = serializer.GetFastBufferWriter();
                int count = FixedItems1.Length;
                writer.WriteValueSafe(count);
                for (int i = 0; i < count; i++)
                {
                    var elem = FixedItems1[i];
                    writer.WriteValueSafe(elem);
                }
            }
            else
            {
                var reader = serializer.GetFastBufferReader();
                reader.ReadValueSafe(out int count);
                FixedItems1.Clear();
                for (int i = 0; i < count; i++)
                {
                    int elem;
                    reader.ReadValueSafe(out elem);
                    FixedItems1.Add(elem);
                }
            }

            // FixedItems2
            if (serializer.IsWriter)
            {
                var writer = serializer.GetFastBufferWriter();
                int count = FixedItems2.Length;
                writer.WriteValueSafe(count);
                for (int i = 0; i < count; i++)
                {
                    var elem = FixedItems2[i];
                    writer.WriteValueSafe(elem);
                }
            }
            else
            {
                var reader = serializer.GetFastBufferReader();
                reader.ReadValueSafe(out int count);
                FixedItems2.Clear();
                for (int i = 0; i < count; i++)
                {
                    UnityEngine.Vector3 elem;
                    reader.ReadValueSafe(out elem);
                    FixedItems2.Add(elem);
                }
            }

            // FixedItems3
            if (serializer.IsWriter)
            {
                var writer = serializer.GetFastBufferWriter();
                int count = FixedItems3.Length;
                writer.WriteValueSafe(count);
                for (int i = 0; i < count; i++)
                {
                    var elem = FixedItems3[i];
                    elem.NetworkSerialize(serializer);
                }
            }
            else
            {
                var reader = serializer.GetFastBufferReader();
                reader.ReadValueSafe(out int count);
                FixedItems3.Clear();
                for (int i = 0; i < count; i++)
                {
                    SimpleData elem = default;
                    elem.NetworkSerialize(serializer);
                    FixedItems3.Add(elem);
                }
            }
        }

        public bool Equals(TestNestedData other)
        {
            return Health == other.Health && Weight == other.Weight && IsAlive == other.IsAlive && Position == other.Position && Rotation == other.Rotation && TransportMessageType == other.TransportMessageType && Name == other.Name && FixedItems1.Equals(other.FixedItems1) && FixedItems2.Equals(other.FixedItems2) && FixedItems3.Equals(other.FixedItems3);
        }
    }
